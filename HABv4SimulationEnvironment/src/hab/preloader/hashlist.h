/*
 * hashlist.h - Empty hash list for HAB PreLoader
 *
 * The efitools PreLoader supports embedding pre-approved hashes.
 * For HAB, we rely on MOK enrollment instead, so this is empty.
 */

unsigned char _tmp_tmp_hash[] = {};
unsigned int _tmp_tmp_hash_len = 0;
